# Core UI + data stack
streamlit>=1.43.0,!=1.59.1,<2.0  # need st.dialog + st.chat_input(accept_file=...)
# 1.59.1 (released 2026-07-08) has a crash bug that takes the whole app down
# on Community Cloud (see discuss.streamlit.io topic 121922); resolves to
# 1.59.0 until a fixed release ships.
pandas>=2.2.0,<3.0
# Streamlit Cloud ships pip 24.0, which cannot parse Metadata-2.4 wheels and
# silently discards every tzdata 2025.x candidate, making pandas' transitive
# requirement tzdata>=2022.7 unsolvable (ResolutionImpossible). Pin a release
# that still uses the old metadata format. (ASCII-only comments here: old pip
# decodes this file with the system locale, not UTF-8.)
tzdata==2024.2
plotly>=5.20.0
pyarrow>=15.0.0          # required by pd.read_parquet

# File format support
openpyxl>=3.1.2          # required by pd.read_excel for .xlsx
duckdb>=0.10.0           # optional: large CSV streaming, falls back to pandas
chardet>=5.2.0           # optional: encoding detection, falls back to common codecs

# AI
openai>=1.30.0           # DeepSeek client uses the OpenAI-compatible SDK

# Citation verification APIs
requests>=2.31.0
tenacity>=8.2.3
diskcache>=5.6.3         # optional: persistent HTTP cache, falls back to in-memory
pyyaml>=6.0.1            # rule config import/export
rapidfuzz>=3.6.1         # optional: title similarity, falls back to difflib

# Report export
jinja2>=3.1.3
reportlab>=4.2.0

# Config
python-dotenv>=1.0.1

# Dev tools (pytest/ruff) live in requirements-dev.txt so cloud deploys and
# Docker images stay lean.
