# Core UI + data stack
streamlit>=1.43.0,<2.0  # need st.dialog + st.chat_input(accept_file=...)
pandas>=2.2.0,<3.0
plotly>=5.20.0
pyarrow>=15.0.0          # required by pd.read_parquet

# File format support
openpyxl>=3.1.2          # required by pd.read_excel for .xlsx
duckdb>=0.10.0           # optional: large CSV streaming, falls back to pandas
chardet>=5.2.0           # optional: encoding detection, falls back to common codecs

# AI / multimodal
openai>=1.30.0           # DeepSeek client uses the OpenAI-compatible SDK
pillow>=10.2.0           # screenshot OCR + agent_router image handling

# Citation verification APIs
requests>=2.31.0
tenacity>=8.2.3
diskcache>=5.6.3         # optional: persistent HTTP cache, falls back to in-memory
pyyaml>=6.0.1            # rule config import/export
rapidfuzz>=3.6.1         # optional: title similarity, falls back to difflib

# Report export
jinja2>=3.1.3
reportlab>=4.2.0
# WeasyPrint requires GTK on Windows; the app degrades gracefully if missing.
weasyprint>=61.0; platform_system != "Windows"

# Config
python-dotenv>=1.0.1

# Tests & static checks (dev)
pytest>=8.0.0
ruff>=0.6.0
